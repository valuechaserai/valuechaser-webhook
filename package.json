{
  "name": "valuechaser-webhook",
  "version": "1.0.0",
  "description": "Webflow to Airtable webhook integration",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": "18.x"
  }
}
